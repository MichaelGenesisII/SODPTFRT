/**
 * Empty stub for Zoom Meeting SDK UMD peer that is not published on npm.
 * Kept for Turbopack resolveAlias safety if anything still references it.
 */
const downloadManager = {};
export default downloadManager;
